[project]
name = "unlock-reso-mcp"
version = "0.1.0"
description = "MCP server implementation with tools, resources, and prompts"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.12.1",
]

[project.scripts]
unlock-reso-mcp = "main:main"
